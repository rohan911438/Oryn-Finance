[package]
name = "access-control"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20.0.0"
oryn-shared = { path = "../shared" }

[dev-dependencies]
soroban-sdk = { version = "20.0.0", features = ["testutils"] }